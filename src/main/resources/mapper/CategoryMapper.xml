<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shike.ordering.mapper.CategoryMapper">
    <resultMap id="CategoryResultMap" type="com.shike.ordering.entity.Category">
        <id property="id" column="id"/>
        <result property="shopId" column="shop_id"/>
        <result property="name" column="name"/>
        <result property="status" column="status"/>
        <result property="sort" column="sort"/>
        <result property="deleted" column="deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 用户端分类查询：只返回当前店铺已启用且未逻辑删除的数据。 -->
    <select id="selectEnabledByShopId" resultMap="CategoryResultMap">
        SELECT c.id, c.shop_id, c.name, c.status, c.sort, c.deleted, c.create_time, c.update_time
        FROM category c
        WHERE c.shop_id = #{shopId}
          AND c.status = 1
          AND c.deleted = 0
        ORDER BY c.sort ASC, c.id ASC
    </select>
</mapper>
