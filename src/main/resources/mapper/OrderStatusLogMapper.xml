<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shike.ordering.mapper.OrderStatusLogMapper">
  <select id="selectByOrderId" resultType="com.shike.ordering.entity.OrderStatusLog">
    SELECT l.id,l.order_id,l.from_status,l.to_status,l.operator_type,l.operator_id,l.reason,
      l.order_version,l.create_time,l.update_time FROM order_status_log l
      WHERE l.order_id=#{orderId} ORDER BY l.order_version,l.id
  </select>
</mapper>
