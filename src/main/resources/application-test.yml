spring:
  datasource:
    url: jdbc:mysql://${TEST_DB_HOST:localhost}:${TEST_DB_PORT:3306}/${TEST_DB_NAME:shike_ordering_test}?serverTimezone=Asia/Shanghai&useSSL=false
    username: ${TEST_DB_USERNAME:shike}
    password: ${TEST_DB_PASSWORD:}
  data:
    redis:
      host: ${TEST_REDIS_HOST:localhost}
      port: ${TEST_REDIS_PORT:6379}
      database: 1
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
